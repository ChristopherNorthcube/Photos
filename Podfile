# Life pods
#
# To update a single pod:
# pod repo update && pod update 'Crashlytics'

platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'

workspace 'Photos'

target 'Photos' do
    pod 'PureLayout'
end

